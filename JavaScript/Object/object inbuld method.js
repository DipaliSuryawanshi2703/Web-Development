let user ={
    name:"dipali",
    Age:25,
    Address:246767,
    contact:5678949489
}
console.log(Object.keys(user));///return oibject as Array Object.keys()
console.log(Object.values(user));//only gives values
console.log(Object.entries(user));//all propertis gives in format of Array e.g[ 'name', 'dipali' ]

